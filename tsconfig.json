{ // used by lint, tests and coverage
  "extends": "./config/tsconfig.base.json",
  "ts-node": { // used only to run tests
    "files": true,
    "compilerOptions": {
      "module": "commonjs",
    }
  },
  "include": [
    "./**/__tests__/*"
  ],
  "references": [
    { "path": "./packages/identity/config/tsconfig.browser.json" }
  ]
}
